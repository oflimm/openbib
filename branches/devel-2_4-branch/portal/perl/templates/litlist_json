[%-

  records = [];

  FOREACH record IN litlist.recordlist.get_records ;
      records.push(record.get_brief_normdata);
  END ;

 thislitlist = {
   meta = litlist.properties
   records = records
 };

to_json(thislitlist) -%]
