[%#-
#######################################################################
#
# Templatename: mainnavigation
#
# Typ         : Subtemplage       
#
# Aufgabe:
#
# Ausgabe der Hauptmenu-Punkte
#
# Parameter active_menu zur Kennzeichnung aktiver Menupunkte:
#
# databasechoice
# searchmask
# resultlists
# collection
# startpage
# rssfeeds
# login
# info
# tutorial
#
#######################################################################
-%]
[%- username = user.get_username -%]
<div id="mainnavigation">
<div id="mainnavigation_left">
&nbsp;&nbsp;<a [%- IF active_menu == "searchmask" -%]class="active_menu" [%- END -%]href="[% path_prefix %]/[% config.get('handler').searchmask_loc.name %]/recent">[% msg.maketext("Suchen") %]</a>&nbsp;&nbsp;<a [%- IF active_menu == "resultlists" -%]class="active_menu" [%- END -%]href="[% path_prefix %]/[% config.get('handler').resultlists_loc.name %]?action=choice">[% msg.maketext("Trefferliste") %]</a>&nbsp;&nbsp;<a [%- IF active_menu == "collection" -%]class="active_menu" [%- END -%]href="[% path_prefix %]/[% config.get('handler').managecollection_loc.name %]?action=show">[% msg.maketext("Merkliste") %]  <span id="collectioncount"></span></a>
</div>
<div id="mainnavigation_right">
[%- IF user.is_admin || session.is_authenticated_as(config.get('adminuser')) -%]<a href="[% path_prefix %]/[% config.get('handler').admin_loc.name %]?do_showcat=1">[% msg.maketext("admin") %]</a>&nbsp&nbsp;[%- END -%]
<a [%- IF active_menu == "startpage" -%]class="active_menu" [%- END -%]href="[% path_prefix %]/[% config.get('handler').info_loc.name %]/20">[% msg.maketext("Startseite") %]</a>&nbsp;&nbsp;
<a [%- IF active_menu == "project" -%]class="active_menu" [%- END -%]href="[% path_prefix %]/[% config.get('handler').info_loc.name %]/30">Projekt</a>&nbsp&nbsp;
<a [%- IF active_menu == "rssfeeds" -%]class="active_menu" [%- END -%]href="http://[% config.get('loadbalancerservername') %][% path_prefix %]/[% config.get('handler').connector_rss_loc.name %]/neuzugang/einbaende.rdf">[% msg.maketext("RSS") %]</a>&nbsp;&nbsp;
<a [%- IF active_menu == "info" -%]class="active_menu"[%- END -%]href="[% path_prefix %]/[% config.get('handler').info_loc.name %]/0">[% msg.maketext("Hilfe") %]</a>&nbsp;::&nbsp;<a href="[% path_prefix %]/[% config.get('handler').leave_loc.name %]?" target="_parent"><b><span class="warning">[% msg.maketext("Sitzung beenden") %]
</span></b></a>
</div>
</div>
