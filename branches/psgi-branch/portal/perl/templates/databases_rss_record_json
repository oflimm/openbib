[%-

  this_json_output = rssinfo;
  this_json_output = {
       link = {
          rel = 'self'
          href = location
       }
       id     => rssinfo.id
       type   => rssinfo.type
       active => rssinfo.active
  };

  to_json(this_json_output);

-%]