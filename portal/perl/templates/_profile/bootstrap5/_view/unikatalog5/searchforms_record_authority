[%- IF representation == '' || representation == 'html' ;
    layout_param = {
     'page_title' = 'Normdaten-Recherche'

      'main_template' = 'searchforms_record_authority_html'
      'sidebar_template' = 'subnavigation'
      'active_menu' = 'searchform'
      'active_submenu' = 'searchform_authority'

      'sidebar_button_in_main_template' = 1
      
      'without_navigation_search' = '1'

      'breadcrumb' = [
          {
             desc = msg.maketext('Start')
	     url  = "${path_prefix}/${config.get('home_loc')}.html?l=${lang}"
	  },
	  {
	     desc = msg.maketext('Normdaten-Recherche')
	     url  = '#'
	     current = 1
	  }
      ]
    };
-%]
[%- PROCESS common/subtemplate name="layout_html" -%]
[%- ELSE -%]
This Resource-Type is not available in the representation [% representation %]
[%- END -%]
