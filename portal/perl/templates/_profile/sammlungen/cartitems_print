[%- IF representation == '' || representation == 'html' ;

    layout_param = {
      'page_title' = 'Druck'
     
      'main_template' = 'cartitems_print_html'
      
    };
-%]
[%- PROCESS common/subtemplate name="layout_html" -%]
[%- ELSE -%]
This Resource-Type is not available in the representation [% representation %]
[%- END -%]
