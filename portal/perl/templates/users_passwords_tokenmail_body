[%-

   encoded_username = uri_encode(username);

   token_url = "${scheme}://${servername}/${path_prefix}/${config.get('users_loc')}/${passwords_loc}/token?l=${lang};token=${token};username=${encoded_username}";

-%]
[% msg.maketext("Sehr geehrte(r) [_1],

Bitte setzen Sie ein neues Passwort über folgenden URL:

[_2]

Mit freundlichen Gruessen

Ihr Portal-Team",username,token_url) %]
