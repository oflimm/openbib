#####################################################################
#
#  openbib.conf
#
#  OpenBib-Konfiguration fuer einen mod_perl-basierten Apache Webserver
#
#  Dieses File ist (C) 2004-2006 Oliver Flimm <flimm@openbib.org>
#
#  Dieses Programm ist freie Software. Sie koennen es unter
#  den Bedingungen der GNU General Public License, wie von der
#  Free Software Foundation herausgegeben, weitergeben und/oder
#  modifizieren, entweder unter Version 2 der Lizenz oder (wenn
#  Sie es wuenschen) jeder spaeteren Version.
#
#  Die Veroeffentlichung dieses Programms erfolgt in der
#  Hoffnung, dass es Ihnen von Nutzen sein wird, aber OHNE JEDE
#  GEWAEHRLEISTUNG - sogar ohne die implizite Gewaehrleistung
#  der MARKTREIFE oder der EIGNUNG FUER EINEN BESTIMMTEN ZWECK.
#  Details finden Sie in der GNU General Public License.
#
#  Sie sollten eine Kopie der GNU General Public License zusammen
#  mit diesem Programm erhalten haben. Falls nicht, schreiben Sie
#  an die Free Software Foundation, Inc., 675 Mass Ave, Cambridge,
#  MA 02139, USA.
#
#####################################################################   

# Ein 'touch' auf openbib.reload sollen einen automatischen Reload
# aller OpenBib-Module im Apache Webserver nach sich ziehen

PerlModule Apache::Reload
PerlInitHandler Apache::Reload

#PerlSetVar ReloadAll       Off
#PerlSetVar ReloadTouchFile /tmp/openbib.reload

# Vorsichtsmassnahme gegen amoklaufende Apache-Prozesse

#PerlSetVar Apache::GTopLimit::DEBUG 1

PerlFixupHandler Apache::GTopLimit

# Konfiguration der einzelnen zum Rechercheportal gehoerenden Module

<Perl>
use OpenBib::Config;

use Log::Log4perl;

use Apache::GTopLimit;

Log::Log4perl->init_once($OpenBib::Config::config{log4perl_path});

$Apache::GTopLimit::MAX_PROCESS_SIZE       = 50000;
$Apache::GTopLimit::CHECK_EVERY_N_REQUESTS = 2;

# Preloading weiterer Module

use DBI               ( );
use Template          ( );
use SOAP::Lite        ( );
use Socket            ( );
use POSIX             ( );
use LWP::UserAgent    ( );
use Email::Valid      ( );
use MIME::Lite        ( );
use IO::Socket        ( );
use HTTP::Request     ( );
use HTTP::Response    ( );
use Digest::MD5       ( );
use Apache::Request   ( );
use Apache::Constants ( );
use XML::RSS          ( );

DBI->install_driver("mysql");

# Initialisierung von OpenBib

push @PerlModule, qw(OpenBib::Admin);

$Location{"$OpenBib::Config::config{admin_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::Admin',
};

push @PerlModule, qw(OpenBib::Circulation);

$Location{"$OpenBib::Config::config{circulation_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::Circulation',
};

push @PerlModule, qw(OpenBib::Search);

$Location{"$OpenBib::Config::config{search_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::Search',
};

push @PerlModule, qw(OpenBib::VirtualSearch);

$Location{"$OpenBib::Config::config{virtualsearch_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::VirtualSearch',
};

push @PerlModule, qw(OpenBib::ResultLists);

$Location{"$OpenBib::Config::config{resultlists_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::ResultLists',
};

push @PerlModule, qw(OpenBib::StartOpac);

$Location{"$OpenBib::Config::config{startopac_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::StartOpac',
};

push @PerlModule, qw(OpenBib::HeaderFrame);

$Location{"$OpenBib::Config::config{headerframe_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::HeaderFrame',
};

push @PerlModule, qw(OpenBib::SearchFrame);

$Location{"$OpenBib::Config::config{searchframe_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::SearchFrame',
};

push @PerlModule, qw(OpenBib::HelpFrame);

$Location{"$OpenBib::Config::config{helpframe_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::HelpFrame',
};

push @PerlModule, qw(OpenBib::ExternalJump);

$Location{"$OpenBib::Config::config{externaljump_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::ExternalJump',
};

push @PerlModule, qw(OpenBib::DispatchQuery);

$Location{"$OpenBib::Config::config{dispatchquery_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::DispatchQuery',
};

push @PerlModule, qw(OpenBib::DatabaseChoice);

$Location{"$OpenBib::Config::config{databasechoice_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::DatabaseChoice',
};

push @PerlModule, qw(OpenBib::Login);

$Location{"$OpenBib::Config::config{login_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::Login',
};

push @PerlModule, qw(OpenBib::LoadBalancer);

$Location{"$OpenBib::Config::config{loadbalancer_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::LoadBalancer',
};

push @PerlModule, qw(OpenBib::Leave);

$Location{"$OpenBib::Config::config{leave_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::Leave',
};
push @PerlModule, qw(OpenBib::ManageCollection);

$Location{"$OpenBib::Config::config{managecollection_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::ManageCollection',
};
push @PerlModule, qw(OpenBib::MailCollection);

$Location{"$OpenBib::Config::config{mailcollection_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::MailCollection',
};
push @PerlModule, qw(OpenBib::MailPassword);

$Location{"$OpenBib::Config::config{mailpassword_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::MailPassword',
};
push @PerlModule, qw(OpenBib::DatabaseProfile);

$Location{"$OpenBib::Config::config{databaseprofile_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::DatabaseProfile',
};

push @PerlModule, qw(OpenBib::SelfReg);

$Location{"$OpenBib::Config::config{selfreg_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::SelfReg',
};

push @PerlModule, qw(OpenBib::UserPrefs);

$Location{"$OpenBib::Config::config{userprefs_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::UserPrefs',
};

push @PerlModule, qw(OpenBib::ServerLoad);

$Location{"$OpenBib::Config::config{serverload_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::ServerLoad',
};

push @PerlModule, qw(OpenBib::Connector::OLWS);

$Location{"$OpenBib::Config::config{connector_olws_loc}"} = {
        SetHandler  => 'perl-script',
        PerlHandler => 'OpenBib::Connector::OLWS',
};

push @PerlModule, qw(OpenBib::Connector::DigiBib);

$Location{"$OpenBib::Config::config{connector_bibmanager_loc}"} = {
        SetHandler  => 'perl-script',
        PerlHandler => 'OpenBib::Connector::DigiBib',
};

$Location{"$OpenBib::Config::config{connector_digibib_loc}"} = {
        SetHandler  => 'perl-script',
        PerlHandler => 'OpenBib::Connector::DigiBib',
};

$Location{"$OpenBib::Config::config{connector_ukonline_loc}"} = {
        SetHandler  => 'perl-script',
        PerlHandler => 'OpenBib::Connector::DigiBib',
};

push @PerlModule, qw(OpenBib::Connector::PermaLink);

$Location{"$OpenBib::Config::config{connector_permalink_loc}"} = {
        SetHandler  => 'perl-script',
        PerlHandler => 'OpenBib::Connector::PermaLink',
};

push @PerlModule, qw(OpenBib::Connector::RSS);

$Location{"$OpenBib::Config::config{connector_rss_loc}"} = {
        SetHandler  => 'perl-script',
        PerlHandler => 'OpenBib::Connector::RSS',
};

push @PerlModule, qw(OpenBib::Connector::UnAPI);

$Location{"$OpenBib::Config::config{connector_unapi_loc}"} = {
        SetHandler  => 'perl-script',
        PerlHandler => 'OpenBib::Connector::UnAPI',
};

push @PerlModule, qw(OpenBib::RSSFrame);

$Location{"$OpenBib::Config::config{rssframe_loc}"} = {
        SetHandler  => 'perl-script',
        PerlHandler => 'OpenBib::RSSFrame',
};

push @PerlModule, qw(OpenBib::FrameSet);

$Location{"$OpenBib::Config::config{frameset_loc}"} = {
        SetHandler  => 'perl-script',
        PerlHandler => 'OpenBib::FrameSet',
};

push @PerlModule, qw(OpenBib::Tags);

$Location{"$OpenBib::Config::config{tags_loc}"} = {
        SetHandler  => 'perl-script',
        PerlHandler => 'OpenBib::Tags',
};

push @PerlModule, qw(OpenBib::Redirect);

$Location{"$OpenBib::Config::config{redirect_loc}"} = {
        SetHandler  => 'perl-script',
        PerlHandler => 'OpenBib::Redirect',
};

push @PerlModule, qw(OpenBib::UserReviews);

$Location{"$OpenBib::Config::config{userreviews_loc}"} = {
        SetHandler  => 'perl-script',
        PerlHandler => 'OpenBib::UserReviews',
};

push @PerlModule, qw(OpenBib::LitLists);

$Location{"$OpenBib::Config::config{litlists_loc}"} = {
        SetHandler  => 'perl-script',
        PerlHandler => 'OpenBib::LitLists',
};

</Perl>
