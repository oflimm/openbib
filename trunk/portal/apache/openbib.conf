#####################################################################
#
#  openbib.conf
#
#  OpenBib-Konfiguration fuer einen mod_perl-basierten Apache Webserver
#
#  Dieses File ist (C) 2004 Oliver Flimm <flimm@openbib.org>
#
#  Dieses Programm ist freie Software. Sie koennen es unter
#  den Bedingungen der GNU General Public License, wie von der
#  Free Software Foundation herausgegeben, weitergeben und/oder
#  modifizieren, entweder unter Version 2 der Lizenz oder (wenn
#  Sie es wuenschen) jeder spaeteren Version.
#
#  Die Veroeffentlichung dieses Programms erfolgt in der
#  Hoffnung, dass es Ihnen von Nutzen sein wird, aber OHNE JEDE
#  GEWAEHRLEISTUNG - sogar ohne die implizite Gewaehrleistung
#  der MARKTREIFE oder der EIGNUNG FUER EINEN BESTIMMTEN ZWECK.
#  Details finden Sie in der GNU General Public License.
#
#  Sie sollten eine Kopie der GNU General Public License zusammen
#  mit diesem Programm erhalten haben. Falls nicht, schreiben Sie
#  an die Free Software Foundation, Inc., 675 Mass Ave, Cambridge,
#  MA 02139, USA.
#
#####################################################################   

# Aenderungen an den Moduldateien sollen einen automatischen Reload
# im Apache Webserver nach sich ziehen

PerlInitHandler Apache::Reload

# Konfiguration der einzelnen zum Rechercheportal gehoerenden Module

<Perl>
use OpenBib::Config;

push @PerlModule, qw(OpenBib::Admin);

$Location{"$OpenBib::Config::config{admin_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::Admin',
};

push @PerlModule, qw(OpenBib::Search);

$Location{"$OpenBib::Config::config{search_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::Search',
};

push @PerlModule, qw(OpenBib::VirtualSearch);

$Location{"$OpenBib::Config::config{virtualsearch_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::VirtualSearch',
};

push @PerlModule, qw(OpenBib::StartOpac);

$Location{"$OpenBib::Config::config{startopac_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::StartOpac',
};

push @PerlModule, qw(OpenBib::HeaderFrame);

$Location{"$OpenBib::Config::config{headerframe_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::HeaderFrame',
};

push @PerlModule, qw(OpenBib::SearchFrame);

$Location{"$OpenBib::Config::config{searchframe_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::SearchFrame',
};

push @PerlModule, qw(OpenBib::DatabaseChoice);

$Location{"$OpenBib::Config::config{databasechoice_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::DatabaseChoice',
};

push @PerlModule, qw(OpenBib::Login);

$Location{"$OpenBib::Config::config{login_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::Login',
};

push @PerlModule, qw(OpenBib::LoadBalancer);

$Location{"$OpenBib::Config::config{loadbalancer_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::LoadBalancer',
};

push @PerlModule, qw(OpenBib::Leave);

$Location{"$OpenBib::Config::config{leave_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::Leave',
};
push @PerlModule, qw(OpenBib::ManageCollection);

$Location{"$OpenBib::Config::config{managecollection_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::ManageCollection',
};
push @PerlModule, qw(OpenBib::MailCollection);

$Location{"$OpenBib::Config::config{mailcollection_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::MailCollection',
};
push @PerlModule, qw(OpenBib::MailPassword);

$Location{"$OpenBib::Config::config{mailpassword_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::MailPassword',
};
push @PerlModule, qw(OpenBib::DatabaseProfile);

$Location{"$OpenBib::Config::config{databaseprofile_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::DatabaseProfile',
};

push @PerlModule, qw(OpenBib::SelfReg);

$Location{"$OpenBib::Config::config{selfreg_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::SelfReg',
};

push @PerlModule, qw(OpenBib::UserPrefs);

$Location{"$OpenBib::Config::config{userprefs_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::UserPrefs',
};

push @PerlModule, qw(OpenBib::ServerLoad);

$Location{"$OpenBib::Config::config{serverload_loc}"} = {
	SetHandler  => 'perl-script',
	PerlHandler => 'OpenBib::ServerLoad',
};

</Perl>
